// src/components/MemberDashboard.js
import React from 'react';
import { Heading, Text} from '@chakra-ui/react';

const MemberDashboard = () => {
    return (
        <div>
            <Heading as="h2" mb="4">
                Your Tempest World
            </Heading>
            <Text mb="8">Track bookings, discover new performers, and stay updated on exclusive events.</Text>
            {/* Add dashboard features and information */}
        </div>
    );
};

export default MemberDashboard;

// Repeat the above structure for other components...
